# instrument-lessons 設計仕様

作成日: 2026-09-10
対象: 楽器の独習教材サイト（第1弾: ウクレレ）

## 1. 目的と成功条件

**目的**: 楽器に触れたことのない人が、独習だけで「弾き語りで3曲を通して演奏できる」状態に到達するための教材を、Web サイトとして提供する。

**最初の利用者**: 本リポジトリのオーナー（ウクレレ所持済み、楽器経験なし、譜面は読めない前提）。

**成功条件**: 利用者が Lesson 01 から順に進み、課題曲3曲（聖者の行進 / Aloha ʻOe / Kuʻu Pua i Paoakalani）を、止まらずに歌いながら最後まで演奏できる。想定期間は 1日15分 × 約5週。

**成功条件ではないもの**: 譜面が読めるようになること、音楽理論の体系的理解、他人に教えられる水準。

## 2. 前提

- 利用者はウクレレを所持済み。機種は未確定のため、教材は標準的な **ソプラノ／コンサート、GCEA（High-G, re-entrant）** を前提に書く。Low-G の場合の差分は注記で補う。
- 五線譜は使わない。**コードダイアグラムと歌詞コード譜**のみで完結させる。
- 利用者は日本語話者。サイトの言語は日本語。
- 閲覧はスマートフォンと PC の両方。練習中はスマホを譜面台代わりに見る想定。

## 3. スコープ

### 作るもの

- ウクレレの全15レッスン（本文 + 図 + 練習ツール）
- 課題曲3曲の演奏用ページ（歌詞コード譜、コード図、テンポ可変の伴奏再生）
- 楽譜ライブラリ（PD ハワイアン曲を中心に15曲、進捗連動フィルタつき）
- 練習ツール7種（§8）
- 簡易な進捗記録（§10）
- PWA 対応（スマートフォンへのインストールとオフライン利用、§11）
- 「人と合わせる」ステージの Coming Soon 枠（見出しと予定内容のみ、中身は空）
- 楽器を後から追加できる構造（§11）

### 作らないもの（意図的に除外）

| 除外するもの | 理由 |
|---|---|
| マイクによる音高検出チューナー | ウクレレ v1 には不要。ただし後付けできる構造にする（§9） |
| 進捗のエクスポート／クラウド同期 | localStorage で足りる。ブラウザ変更で消える旨を明記 |
| ユーザー登録・認証 | 単一利用者の静的サイト |
| 市販曲の歌詞・コード譜の掲載 | 著作権（§6） |
| 弦楽器共通キット（`src/kit/strings/`） | 楽器1つの段階で共有形を推測すると外す。ギター追加時に ukulele/ から引き上げる |
| 五線譜・TAB譜の表示 | 弾き語りの到達目標には不要 |

## 4. カリキュラム

全15レッスン。1レッスン = 1日15分で終わる分量に切る。

### コード導入順

課題曲から逆算して決めた。**新しいコードは、それを使う課題曲の直前に導入する**。

| 導入 | コード | 押さえ方（4弦G→1弦A） | 使う課題曲 |
|---|---|---|---|
| Stage 1 | C | 0 0 0 3 | 全曲 |
| Stage 2 | F | 2 0 1 0 | 全曲 |
| Stage 2 | G7 | 0 2 1 2 | 全曲 |
| Stage 3 | C7 | 0 0 0 1 | Kuʻu Pua i Paoakalani |
| Stage 4 | D7 | 2 2 2 3 | Kuʻu Pua i Paoakalani（1小節で2回替わる箇所） |
| Stage 4（応用） | Am, Em, Dm, A7 | 2 0 0 0 / 0 4 3 2 / 2 2 1 0 / 0 1 0 0 | 外部コード譜を読むため |

### レッスン一覧

| # | ステージ | タイトル | 日 | 新コード | 到達点 |
|---|---|---|---|---|---|
| 01 | 0 準備 | ウクレレの各部と持ち方 | 1 | — | 構えが安定し、音が出る |
| 02 | 0 準備 | チューニング | 2 | — | 4本の弦を自力で合わせられる |
| 03 | 1 最初の音 | 右手：弦を1本ずつ鳴らす | 3 | — | 4本すべてを均等に鳴らせる |
| 04 | 1 最初の音 | ダウンストロークと4拍のカウント | 4-5 | — | メトロノームに合わせて4拍刻める |
| 05 | 1 最初の音 | はじめてのコード C | 6-7 | C | C を鳴らしながら4拍刻める |
| 06 | 2 コードを増やす | F と、コードチェンジの練習法 | 8-10 | F | C↔F を1小節ごとに替えられる |
| 07 | 2 コードを増やす | G7 とスリーコード | 11-13 | G7 | C-F-G7 を止まらず回せる |
| 08 | 2 コードを増やす | **課題曲① 聖者の行進** | 14-16 | — | 1曲を通して演奏できる |
| 09 | 3 リズム | アップストロークと8ビート | 17-19 | — | ↓↑ を均等に刻める |
| 10 | 3 リズム | 定番パターン D-DU-UDU | 20-22 | C7 | パターンを保ったままコードを替えられる |
| 11 | 3 リズム | **課題曲② Aloha ʻOe** | 23-25 | — | ストロークパターンで1曲通る |
| 12 | 4 歌と合わせる | 歌詞にコードを乗せて読む | 26-27 | — | コード譜を見て演奏できる |
| 13 | 4 歌と合わせる | ハワイアン・ヴァンプと D7 | 28-30 | D7 | D7-G7-C を2拍2拍4拍で弾ける |
| 14 | 4 歌と合わせる | **課題曲③ Kuʻu Pua i Paoakalani** | 31-33 | — | 1小節で2回替わる曲を通せる |
| 15 | 4 歌と合わせる | よく出るコードと、止まらずに通す練習法 | 34-35 | Am, Em, Dm, A7 | 外部のコード譜を自力で使える |
| — | 5 人と合わせる | **Coming Soon** | — | — | 枠のみ |

### Stage 5（Coming Soon 枠）に載せる予定項目

見出しと1行説明だけを置き、本文は「準備中」とする。

- 移調とカポ：歌いやすいキーへ移す
- イントロ／エンディングとターンアラウンド
- 他の楽器・歌に合わせる伴奏
- 耳コピ入門

## 5. 楽器の抽象化

### 共通契約は薄くする

将来追加する楽器に**オタマトーン**（フレットなし・連続音程・コードの概念なし）が含まれるため、共通の楽器契約に弦・チューニング・コード形を持たせない。

```ts
// src/core/lesson/types.ts
export type Instrument = {
  id: string;          // "ukulele"
  slug: string;        // URL: /ukulele/
  name: string;        // "ウクレレ"
  tagline: string;
  curriculum: Curriculum;
};
```

コードダイアグラム、ストロークパターン、チューニングは**ウクレレモジュールの内部**に閉じる。

### ディレクトリ構成

```
src/
  core/
    lesson/        レッスン・カリキュラムの型、ルーティング、MDXレイアウト
    audio/
      output/      音の合成・再生・スケジューラ
      input/       （空。将来のマイク入力・音高検出用の置き場）
      pitch.ts     音名↔周波数（12平均律, A4=440）
    widgets/       Metronome, MelodyPlayer, TonePlayer
    progress/      進捗ストア
  instruments/
    registry.ts    楽器の登録
    ukulele/
      instrument.ts  メタ情報
      tuning.ts      GCEA
      chords.ts      コード定義
      curriculum.ts  レッスン一覧・課題曲
      widgets/       ChordDiagram, StrumPattern, ChordChangeTrainer, SongSheet
      songs/         曲データ（1曲1ファイル）と権利情報の型
      pages/         楽譜ライブラリの一覧・個別ページ
content/
  ukulele/
    lesson-01.mdx 〜 lesson-15.mdx
```

曲データは `content/` ではなく `src/instruments/ukulele/songs/` に置く（2026-09-11 改訂）。
`content/` は MDX 本文の置き場で、曲データは型検査とテストの対象になる値のため。

`core/audio/pitch.ts`・メトロノーム・メロディ再生は、オタマトーンでも確実に使うため最初から core に置く。それ以外の共有は、2つ目の楽器が来てから引き上げる。

### オタマトーン追加時に何が起きるかの検証

| 必要になるもの | 既存で足りるか |
|---|---|
| レッスン基盤・ルーティング・進捗 | ✅ core をそのまま使う |
| メトロノーム | ✅ core |
| 音名↔周波数、お手本メロディ再生 | ✅ core |
| 音高リボンの図 | ❌ `instruments/otamatone/widgets/` に新規追加 |
| マイクによる音程判定 | ❌ `core/audio/input/` に新規追加（構造は用意済み） |
| コードダイアグラム | 不要（ウクレレ側に閉じているので邪魔にならない） |

## 6. 練習曲・楽譜ライブラリと著作権

### 方針

1. 練習用フレーズは自作する
2. サイト内に掲載する曲は**日本と米国の両方で保護期間が満了した曲**に限る
3. 有名曲は曲名を挙げて外部コード譜サイトへリンクするに留め、歌詞・コード譜は転載しない

### 判定基準（二重基準）

サイトは GitHub Pages（米国ホスト）で公開するため、日本法だけの判定では足りない。次の両方を満たす曲だけを掲載する。

- **日本** — 作詞者・作曲者の**両方**が1967年12月31日までに死亡している（または伝承曲）
- **米国** — 初出版が1929年より前である

日本の保護期間は 2018-12-30 の TPP11 協定発効で「著作者の死後70年」へ延長されたが、施行日時点で消滅していた著作権は復活しない（保護の不遡及）。文化庁の Q&A により、1967年12月31日までに死亡した著作者の作品は満了済みで確定している。

米国側は、1929年より前に出版された作品が PD となる。日本で PD であっても米国の保護が残る例もあるため、この条件を別途課す。

### 除外した実例

判定を誤りやすい例を記録として残す。

| 曲 | 除外理由 |
|---|---|
| 夕焼小焼 | 作詞の中村雨紅が1972年没。2042年まで保護 |
| 椰子の実 | 作曲の大中寅二が1982年没。2052年まで保護 |
| 赤とんぼ | 日本では PD だが、1927年発表のため米国の保護が継続中 |

作曲者だけを見て判断すると事故る。**作詞・作曲の両方**を確認する。

### 課題曲

| 曲 | 作者 | 没年 | 判定 | 使用コード |
|---|---|---|---|---|
| When the Saints Go Marching In（聖者の行進） | 作者不詳（黒人霊歌、19世紀） | — | 伝承曲・PD | C, F, G7 |
| Aloha ʻOe | Queen Liliʻuokalani | 1917 | 1967年以前に没 → PD | C, C7, F, G7 |
| Kuʻu Pua i Paoakalani | Queen Liliʻuokalani（1895年作・1917年出版の King 編曲版） | 1917 | 1967年以前に没 → PD | C, C7, D7, F, G7 |

歌詞も同じ根拠で掲載可。

**課題曲③は 2026-09-12 に Kaimana Hila から差し替えた**（DDR 019）。今よく歌われている Kaimana Hila は
Andy Cummings が広めた別の版（著作権表示 1943）で、原曲とは旋律も歌詞も違う。米国側の条件を満たさないため載せられない。
Kaimana Hila は、楽譜ライブラリに1916年の原曲として残す。

**Kaimana Hila の補作者について（2026-09-11 改訂）。** 当初この表は「Andrew Cummings 補作」と
書いていた。しかし**原譜の作者表記は Chas. E. King 単独**だった。
現物は `King's Book of Hawaiian Melodies` 第5版・1923年の88ページで、
同ページに "Copyright, 1916, by Chas. E. King" の表記もある。
補作の記述は英語版 Wikipedia のスタブに由来し典拠がなく、補作者として名の挙がる Andy Cummings は
1913年生まれで1916年の曲を共作できない。経緯は `docs/songs-licensing.md` が正本。

**この1件が示すこと。** 作者が1人でも追えないと日本側の判定が確定しない。
判定の根拠は二次情報ではなく**原譜と機関の記録**で取る。

### 楽譜ライブラリ

`/ukulele/songs` に、掲載可能な曲を一覧するページを置く。ハワイアン曲を中心に構成する。

v1 の収録目標は15曲（課題曲3曲 + ハワイアン12曲）とする。

**曲数は目標であって、掲載の合格ラインではない**（2026-09-11 追記）。
裏取りを通らない曲は落とし、通った数で公開する。

候補は Live Ukulele の PD リストに100曲以上あり、うち出版年の判明したものが約55曲（すべて1929年より前）。ただし同リストは作者自身が未検証と明記しているため、候補の入口としてのみ使い、掲載する曲は個別に裏取りする。

収録候補（実装時に個別検証し、初心者キーへ移調する）:

- Aloha ʻOe / Kaimana Hila / 聖者の行進（課題曲）
- On the Beach at Waikiki (1915) / Hawaiʻi Ponoʻī (1874) / Kāua i ka Huahuaʻi (1860)
- Sanoe (1870s) / Kuʻu Pua i Paoakalani (1895) / Queen's Prayer (1895)
- Ke Kali Nei Au (1925) / Nā Lei o Hawaiʻi (1915) / Pālolo (1917)
- Pūpū a ʻo ʻEwa (1870s) / Hilo Hanakahi (伝承) / Papalina Lahilahi (伝承)

Ke Kali Nei Au は King によるハワイ語詞のみを掲載する。1958年の英語詞（Hoffman / Manning）は保護期間中のため扱わない。

#### 一覧ページの絞り込み

- **今の自分が弾ける曲** — 進捗記録の習得済みコードだけで弾ける曲に絞る
- コード数（3コード / 4コード / 5コード以上）
- セーハを含むかどうか

#### 権利情報のビルド時強制

曲データに検証記録を必須フィールドとして持たせる。

```ts
export type SongAuthor = {
  name: string;
  role: "lyrics" | "music" | "both";
  died: number | "traditional";
};

export type SongLicensing = {
  /** 分かっている作者を全員。1人でも没年が追えなければ掲載しない。 */
  authors: SongAuthor[];
  /** 確認できた最も古い出版年。伝承曲で特定できない場合は "traditional"。 */
  earliestPublication: number | "traditional";
  /** earliestPublication が "traditional" のときの、米国側の根拠。 */
  usBasis?: string;
  verifiedOn: string;    // "YYYY-MM-DD"
  sources: string[];     // 出典URL、1件以上
  /** 判定に残る疑義。 */
  caveat?: string;
};
```

型は2026-09-11 に改訂した。作者を配列にしたのは、共作曲と補作者を1人に丸めないため。
`firstPublished` を `earliestPublication` へ改めたのは、確認できたのが最古の出版とは限らず、
「初出版」と名乗ると裏取りしていないことを断定してしまうため（米国側の判定には確認できた年で足りる）。

テストで次を検査し、満たさない曲があればビルドを落とす。

- `earliestPublication` が 1929 未満、かつ 1700 以上。`"traditional"` なら `usBasis` が非空
- **作者全員**の没年が 1967 以下、または `"traditional"`
- `sources` が1件以上あり、すべて `https://`
- `verifiedOn` が `YYYY-MM-DD` で、未来でない
- 使うコードが定義済みで名前順。進行・譜面に出るコードの集合が一致
- 課題曲はカリキュラムの記述と一致

同じ記録を `docs/songs-licensing.md` にも人が読める形で残す。曲を追加するときは同ファイルへの追記を必須とする。

### 出典

- 文化庁「著作物等の保護期間の延長に関する Q&A」 https://www.bunka.go.jp/seisaku/chosakuken/hokaisei/kantaiheiyo_chosakuken/1411890.html
- Charles E. King（1874-1950） https://en.wikipedia.org/wiki/Charles_E._King
- Kaimana Hila https://en.wikipedia.org/wiki/Kaimana_Hila
- Live Ukulele「Public Domain Hawaiian Songs」（候補リスト、作者による未検証の注記あり） https://liveukulele.com/songs/public-domain-hawaiian-songs/
- 中村雨紅（1897-1972） https://ja.wikipedia.org/wiki/中村雨紅
- 大中寅二（1896-1982） https://ja.wikipedia.org/wiki/大中寅二
- 武島羽衣（1872-1967） https://ja.wikipedia.org/wiki/武島羽衣

## 7. 技術構成

| 項目 | 採用 |
|---|---|
| ビルド | Vite 8 |
| UI | React 19 + TypeScript |
| コンテンツ | MDX（`@mdx-js/rollup` 3.x + `remark-gfm`） |
| ルーティング | `react-router-dom` 7.x（BrowserRouter + basename） |
| 音 | Web Audio API（外部ライブラリなし） |
| テスト | Vitest + @testing-library/react + jsdom |
| デプロイ | GitHub Pages（GitHub Actions） |

既存の flashcards / zoo-aquarium-log と同じ流儀。CI ワークフローは flashcards の `deploy.yml` を踏襲する（lint → test → build → Pages）。Node は CI で 22 を使う。

### 却下した案

- **Astro + islands** — コンテンツサイトとしては理論上最適だが、既存3プロジェクトと別スタックになるコストが確実に発生する一方、15〜30本規模のレッスンでは静的出力の体感差が出にくい。
- **素の HTML/CSS/JS** — レッスンが増えると共通レイアウトの手作業複製が破綻し、楽器の抽象化ができない。

## 8. 練習ツール仕様

すべて `React` コンポーネントとして MDX 本文中に埋め込む。

| # | 名前 | 置き場所 | 仕様 |
|---|---|---|---|
| 1 | ChordDiagram | ukulele | SVG。4弦×5フレット。押さえる位置を丸、指番号を数字、開放弦を ○、ミュートを ✕ で表示。セーハは横棒 |
| 2 | ChordPlayer | ukulele | ChordDiagram に発音を付ける。ストローク／アルペジオ切替 |
| 3 | StrumPattern | ukulele | ↓↑ と空振りの図。再生すると現在の拍がハイライト。BPM 40-160 |
| 4 | ChordChangeTrainer | ukulele | 2コードを交互に指示。60秒で何回替えられたか計測（記録は保存しない） |
| 5 | SongSheet | ukulele | 歌詞の上にコードを配置。コード名タップで図と音。曲データに再生の情報があれば、伴奏とメロディをテンポ可変で鳴らす（DDR 017） |
| 6 | TonePlayer | core | 指定した音名を鳴らす。チューニング用（4弦分のボタン） |
| 7 | Metronome | core | BPM・拍子・1拍目アクセント。Web Audio のスケジューラで正確に刻む |

### 実装で仕様から外した点（2026-09-11 更新）

2つのツールを実装した時点で、当初の仕様と噛み合わない点が見つかったため仕様側を直した。

- **ChordPlayer の「テンポ指定」を外した** — 単発で鳴らすだけの道具にテンポを持たせても使い道がない。繰り返し鳴らす用途は StrumPattern が引き受ける
- **ChordChangeTrainer の「指定 BPM で交互表示」を「交互に指示」へ変えた** — この練習は決めたテンポに合わせるのではなく、60秒でできるだけ多く替える形（Lesson 06）なので、テンポの指定と矛盾する。テンポに乗せる練習は Metronome と StrumPattern が担う
- **SongSheet の「全体をテンポ可変で伴奏再生」は、いったん外してから別の形で戻した** — この記法はコードが替わる場所しか持たず、各コードの長さを書けない。記法へ小節線や拍数を足すと、Lesson 12 で教える規則が1つ増える。そこで記法は変えず、拍の長さとメロディを曲データの `performance` に ABC 記法のサブセットで持たせた（2026-09-11、DDR 017）。譜面との食い違いはテストで落とす

### 歌詞コード譜の記法

ChordPro 風のインライン記法を採用し、パーサを自作する（テスト対象）。

```
[C]Oh when the saints [F]go marching [C]in
```

小節線・繰り返し・拍の情報は持たせない。Lesson 12 が教える規則は「角かっこの直後の文字でコードを替える」の1つだけで、表示ツールもその1つだけを写す。お手本の再生が使う拍の長さは、曲データの側で別に持つ（DDR 017）。

## 9. 音声エンジン

### 合成方式

**Karplus-Strong 法**で撥弦音を合成する。オシレータの単純な正弦波・矩形波では弦楽器に聞こえず、教材として音程確認以上の役に立たないため。

実装は `OfflineAudioContext` ではなく、Float32Array へ直接生成して `AudioBuffer` に載せ、`AudioBufferSourceNode` で再生する。AudioWorklet より単純で、ブラウザ差の影響も受けにくい。

```
1. 長さ N = round(sampleRate / frequency) のノイズバーストを用意
2. y[n] = damping * 0.5 * (y[n-N] + y[n-N-1]) を繰り返して減衰させる
3. 音量エンベロープを掛けて AudioBuffer 化
```

### 音名↔周波数

12平均律、A4 = 440 Hz 基準。`core/audio/pitch.ts` に置く。

ウクレレ標準チューニング（High-G）の基準周波数:

| 弦 | 音名 | 周波数 |
|---|---|---|
| 4弦 | G4 | 392.00 Hz |
| 3弦 | C4 | 261.63 Hz |
| 2弦 | E4 | 329.63 Hz |
| 1弦 | A4 | 440.00 Hz |

4弦が3弦より高い（re-entrant tuning）ことは Lesson 02 で明示的に説明する。初心者が最も混乱する点のため。

### 入力の分離

`core/audio/` を `output/`（合成・再生）と `input/`（マイク・解析）に分ける。v1 で作るのは `output/` だけとし、オタマトーンで音高検出が必要になった時点で `input/` を作る。空のディレクトリは git に残らないため、あらかじめは作らない。

### ブラウザの制約

Web Audio はユーザー操作なしに開始できない。`AudioContext` は最初のタップ／クリック時に生成し、以降使い回す。この扱いを `core/audio/output/context.ts` に一箇所へ集約する。

## 10. 進捗記録

localStorage のみ。楽器ごとに独立。

```ts
// キー: "instrument-lessons:progress:v1"
type ProgressState = {
  [instrumentId: string]: {
    completedLessonIds: string[];
    lastLessonId: string | null;
    practiceDates: string[];   // "YYYY-MM-DD"
  };
};
```

- レッスン末尾に「完了にする」ボタン（取り消し可）
- コース一覧にチェックマークと進捗バー
- コース概要に「連続◯日練習中」（進捗は楽器ごとに独立するため、トップではなくここへ置く）

`practiceDates` は「完了にする」を押した日を記録する。連続日数はローカル日付で判定する（UTC ではなく `toLocaleDateString` 相当）。

localStorage が使えない環境（プライベートウィンドウ等）では例外を握りつぶし、進捗なしで正常に動作させる。ブラウザを変えると進捗が消える旨をトップに1行明記する。

## 11. ルーティングとデプロイ

| URL | 内容 |
|---|---|
| `/` | 楽器の一覧（現在はウクレレのみ、オタマトーンは Coming Soon） |
| `/ukulele/` | コース概要・レッスン一覧・進捗 |
| `/ukulele/lesson-01` | 各レッスン |
| `/ukulele/songs` | 楽譜ライブラリ（絞り込みつき一覧） |
| `/ukulele/songs/saints` | 個別の曲ページ |
| `/ukulele/chords` | コード一覧（リファレンス） |
| `/ukulele/tools/metronome` 等 | 練習ツール単体ページ |

GitHub Pages のプロジェクトサイト（`/instrument-lessons/`）で動かすため:

- Vite の `base` を `/instrument-lessons/` に設定
- React Router に同じ `basename` を渡す
- ビルド後、`dist/index.html` を `dist/404.html` へコピーし、直リンク・リロードでも 404 を返さないようにする

### PWA

練習中はスマートフォンを譜面台代わりに見るため、ホーム画面から全画面で開けるようにする。`vite-plugin-pwa` を使う（flashcards と同じ構成）。

音は合成するため音源ファイルの取得が要らず、レッスン本文もバンドルに含まれる。したがってオフラインでもレッスンと練習ツールがそのまま動く。

`start_url` と `scope` は `base`（`/instrument-lessons/`）と一致させる。ずれるとインストールできない、あるいは起動時に画面が真っ白になる。

iOS は manifest の icons を参照しないため、`index.html` に `apple-touch-icon` を明示する。

## 12. テスト戦略

Vitest。**教材の破綻を機械検出することを最優先**とする。

| 対象 | テスト内容 |
|---|---|
| 権利情報 | 全曲に `licensing` があり、初出版が1929年より前、作詞・作曲の没年が1967以下または伝承曲、出典が1件以上あるか |
| カリキュラム整合 | 課題曲で使うコードが、その曲までのレッスンで導入済みのコードだけか |
| カリキュラム整合 | レッスンID の重複なし、day の連続性、参照先の MDX が存在するか |
| コード定義 | フレット番号が範囲内、指番号と押さえる弦の整合、弦数がチューニングと一致 |
| pitch.ts | 音名↔周波数の往復、A4=440、オクターブ境界、GCEA の実測値との一致 |
| ChordPro パーサ | コード位置の抽出、コードなしの行、記号のエスケープ |
| 進捗ストア | 完了の追加・取り消し、連続日数の日付境界、localStorage 不在時のフォールバック |
| ChordDiagram | 開放弦・ミュート・セーハの描画 |

音の実際の鳴りは自動テストしない（目視ならぬ耳視で確認する）。

## 13. デザイン方針

実装時に `frontend-design` スキルを呼んで視覚方針を決める。制約として:

- 練習中にスマホで見るため、**コードダイアグラムは腕を伸ばした距離で読める大きさ**を最優先する
- ハワイアンの空気感は配色でほのかに漂わせる程度とし、学習サイトとしての可読性を優先する
- 楽器が増えても破綻しないよう、楽器ごとのアクセントカラーをデータで持つ

## 14. 未確定事項

| 項目 | 現在の仮定 | 外れた場合の影響 |
|---|---|---|
| ウクレレの機種・サイズ | ソプラノ／コンサート、High-G | Low-G だった場合、Lesson 02 の弦の説明と一部のコードの響きの記述を修正（構造への影響なし） |
| GitHub のリポジトリ名の空き | `iiiitiiitiiti/instrument-lessons` が空いている | 埋まっていればリポジトリ名と `base` を変更 |
